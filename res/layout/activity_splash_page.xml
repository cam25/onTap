<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:background="@drawable/texturebkg" >

    <ImageView
        android:id="@+id/logo"
        android:layout_width="fill_parent"
        android:layout_height="325dp"
        android:contentDescription="@string/todo"
        android:src="@drawable/logo" 
        android:layout_centerHorizontal="true"/>

    <ImageView
        android:id="@+id/logotext"
        android:layout_width="match_parent"
        android:layout_height="325dp"
        android:layout_alignLeft="@+id/logo"
        android:layout_alignTop="@+id/logo"
        android:contentDescription="@string/todo"
        android:src="@drawable/logotext"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/disclaimer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/logo"
        android:layout_alignParentBottom="true"
        android:text="This application does not support under-age drinking so to help alleviate minors obtaining information on alcoholic beverages please vefiy your birthdate." 
        android:textAppearance="?android:attr/textAppearanceLarge" />

</RelativeLayout>